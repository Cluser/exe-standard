import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'client-mobile',
  webDir: '../../../dist/apps/client-mobile',
  bundledWebRuntime: false,
};

export default config;
